import React from 'react';
import {Alert} from 'reactstrap'

const ErrorMessage = () => {
    return (
        <Alert color="danger">
        Something goes wrong...
        </Alert>
    )
}

export default ErrorMessage;